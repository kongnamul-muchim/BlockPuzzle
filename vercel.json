{
  "version": 2,
  "builds": [
    {
      "src": "Server/api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "Server/api/$1"
    }
  ]
}
